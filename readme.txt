// npm install should install everything automatically as they are in the pakcage.json file 
// this is a list of specific packages should you want to do this manually.

i ran the following:

npm install express
npm install ejs
npm install nodemon